{
  "title": "AI Workflow Builder MVP",
  "panels": [
    {
      "type": "graph",
      "title": "API p95 latency",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(http_server_requests_seconds_bucket[5m])) by (le))"
        }
      ]
    },
    {
      "type": "graph",
      "title": "API error rate",
      "targets": [
        {
          "expr": "sum(rate(http_requests_total{status=~\"5..\"}[5m]))"
        }
      ]
    }
  ],
  "schemaVersion": 38
}